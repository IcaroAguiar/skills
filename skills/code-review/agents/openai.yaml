interface:
  display_name: "Code Review"
  short_description: "Standards e Spec em um único diff"
  default_prompt: "Use $code-review para revisar este diff fixo contra os standards do repositório e a especificação, sem abrir reviewers aninhados."
policy:
  allow_implicit_invocation: true
