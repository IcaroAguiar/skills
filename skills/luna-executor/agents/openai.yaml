interface:
  display_name: "Luna Executor"
  short_description: "Governed Multiagents v2 delivery waves"
  default_prompt: "Use $luna-executor to deliver a governed wave of bounded packages with native Luna subagents by default or Terra only when escalation is justified. Coordinate independent writers on one primary-checkout integration branch and PR through disjoint ownership and serialized Git turns; use a worktree when real isolation or an independent branch/PR is required."
