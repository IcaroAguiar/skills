interface:
  display_name: "Luna Executor"
  short_description: "One governed package in the primary checkout"
  default_prompt: "Use $luna-executor to deliver one bounded package in the primary checkout with a native Luna subagent by default or Terra only when escalation is justified. Use a worktree only for real parallel work, then return runtime and user testing to the primary checkout before the human gate."
