interface:
  display_name: "Luna Executor"
  short_description: "One cheap task from fix through cleanup"
  default_prompt: "Use $luna-executor to deliver one bounded package in a dedicated Luna task, with an isolated worktree, evidence handoff, audit, cleanup, and a human gate."
